{
  "name": "is-sw",
  "version": "1.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "cp package.json dist/ && cp README.md dist/ && cp LICENSE dist/",
    "publish:package": "npm run build && npm publish dist"
  },
  "author": "arbrim",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}